<Page
    x:Class="HostApp.ChooseSourceApp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HostApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interop="using:Windows.UI.Xaml.Interop"
    xmlns:viewModel="using:HostApp.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="App Repurposing Utility" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Source" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
                    Select the application from the list below you wish to use as the source.
                </TextBlock>
            </StackPanel>
            <ListBox Background="Black" ItemsSource="{Binding Apps}" Grid.Row="1" 
                     SelectedItem="{Binding SelectedApp,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <ListBox.ItemTemplate>
                    <DataTemplate >
                        <StackPanel>
                            <TextBlock Text="{Binding}"
                                   FontSize="19"
                                   Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"
                                   Foreground="LightYellow">

                            </TextBlock>
                            <HyperlinkButton FontSize="14" HorizontalAlignment="Left"
                                             Command="{Binding ElementName=ContentRoot,Path=DataContext.ShowManifestCommand}"
                                             CommandParameter="{Binding}">Show Manifest</HyperlinkButton>
                        </StackPanel>
                       
                      
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" Grid.Row="2" 
                        VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Button x:ConnectionId='1' Width="100"                    >Next</Button>
            </StackPanel>
        
        </Grid>
    </Grid>
</Page>

